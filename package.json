{
    "name": "evoting",
    "version": "1.0.0",
    "description": "Blockchain-based E-Voting System - Bharat E-Vote",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.js --network localhost",
        "node": "hardhat node"
    },
    "dependencies": {
        "dotenv": "^17.4.2"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^6.1.2",
        "hardhat": "^2.22.0"
    }
}
